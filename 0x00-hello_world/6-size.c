#include<stdio.h>
/**
 * main - is the entry point of the program
 * Return: Always 0 is success
 */

int main(void)
{
	sizeof(int);
	sizeof(char);
	sizeof(long int);
	sizeof(long long int);
	sizeof(float);
	return (0);

}
