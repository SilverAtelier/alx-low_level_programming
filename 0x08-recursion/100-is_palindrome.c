#include "main.h"
/**
 * is_palindrome - check if palindrome
 * @s: string to check
 * Return: 1 if palindrome, 0 if not
 */

int _return_f(void){
    char *s;
    int a;
    a = is_palindrome(*s);
    return (a);
}
