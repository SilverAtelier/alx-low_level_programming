#include "main"
/**
 * _abs-compute absolute value of integer
 * Return:-absolute value
 */
int _abs(int n)
{
	num = n;

	if (num > 0)
	{
		return (num);
	}
	else if (num < 0)
	{
		return (-num);
	}
	return (0);
}
