#include <stdarg.h>
#include "variadic_functions.h"
#include <stdio.h>
/**
 * print_all - prints anything.
 * @format: input
 *
 */
void print_all(const char * const format, ...)
{
	va_list ap;

	while (ap != NULL)
	{
		if

	va_start(ap,format);
	va_end(ap);
}
