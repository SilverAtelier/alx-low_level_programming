#include "main.h"

/**
 * _strlen-returns the length of a string.
 * Return:string length
 * @s:string input
 */

int _strlen(char *s)
{
	int mystr, a;

	mystr = *s;
	a = sizeof(mystr);
	
	return (a);
}
